package youtube.youtubeProject.trashcan;


//@Slf4j
//@RestController
//@RequiredArgsConstructor
//@RequestMapping("/youtube")
public class YoutubeController {

    /*private final YoutubeService youtubeService;

    //@GetMapping
    public ResponseEntity<String> searchIdByHandler() throws IOException {
        // not done
        String result = youtubeService.searchChannelIdThruHandler();
        return ResponseEntity.ok(result);
    }

    @GetMapping
    public ResponseEntity<String> searchPlaylist() throws IOException {
        // not done
        String result = youtubeService.searchPlaylist();
        return ResponseEntity.ok(result);
    }

    //@GetMapping
    public ResponseEntity<String> searchVideo(@RequestParam String keyword) throws IOException {
        // YoutubeService를 통해 동영상 검색한 결과를 받아옴
        // 작동함
        String result = youtubeService.searchVideo(keyword);
        return ResponseEntity.ok(result);
    }

    //@GetMapping
    public String search(@RequestParam String keyword) {
        return "검색 키워드: " + keyword;
    }*/
}